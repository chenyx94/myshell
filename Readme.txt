This is a simple shell could run on linux. The prompt of this shell is the date and specific time get from the system. The command this simple shell supports are ‘ls’, ‘ls -l’, ’echo’, ‘exit’. While the user enter the wrong command, the shell will return the error code and information. While the user enter nothing, the shell will directly go to the next line.